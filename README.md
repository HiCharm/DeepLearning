# DeepLearning

这个项目主要是用来学习深度学习，会包含工具库，数据集，模型算法。一步一步，逐渐成长。

## 单元主要组成部分

数据集部分，会由专门的存储数据类，以及对应的操作的静态函数来控制

数据结构单元部分，基础数据结构以向量为基本单元。

各种函数操作也是以向量来进行操作。

由于数据结构特性，一般的矩阵由xy来定位，x为行，y为列。

但这样不做适配器的话，会很难统一格式，所以进行封装，统一使用get和set方法操作。
